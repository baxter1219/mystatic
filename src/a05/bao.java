package a05;

public class bao {
    /*
     * 包就是文件夹，用于管理不同的java类
     * 命名：公司域名的反写+包的作用
     * 例如：com.baodu.domain + 类名（全类名）
     * 导包的情况：
     * 使用本包的类是时候，不用导包
     * 使用java.lang包中的类时，不需要导包
     * 其他情况需要导包
     * 使用两个包中的同名类，需要使用全类名
     */

    /*
     * final:
     * 修饰方法：表示方法是最终的方法，不能重写
     * 修饰类：不能被继承
     * 修饰变量：叫做常量，不能被再被赋值（只能赋值一次）
     */
}
